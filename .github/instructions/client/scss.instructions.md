---
applyTo: "sources/**/*.scss"
---

## Scss Conventions

- Use `mixins.rem` or `mixins.em` rather than `px` for font sizes, spacing, and other properties that should scale with the user's settings.

## Coding Style

- Always order the properties in a selector block alphabetically.
